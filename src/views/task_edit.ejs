<% include partials/head.ejs %>
<p></p>
   <div class="container mt-5">
       <div class="row">
           <div class="col-md-5">
               <div class="card">
                  <div class="card-header">
                    edit task
                  </div>
                   <div class="card-body">
                       <form action="/update/<%= datos.id %>" method="post">
                        
                        <div class="form-group">
                            <input type="text" name="title" placeholder="insert title" class="form-control"
                            value="<%= datos.title %>">
                        </div>

                        <div class="form-group">
                              <textarea name="description"  rows="3" class="form-control" placeholder="insert description"
                              > <%= datos.description %> </textarea>
                        </div>
                         <button type="submit" class="btn btn-warning btn-block">edit Task</button>
                       </form>
                   </div>
               </div>
            </div>
        </div>
        </div>

        <% include partials/footer.ejs %>